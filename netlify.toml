[build]
  command = "cd neonpay && npm install && npm run build"
  publish = "neonpay/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
